# multi-indiegame external protocol

Akashic Engine のコンテンツ拡張と、それを受け入れるコンテンツ実行基盤との間の通信仕様。

版: **1**（この文書全体の版であって、各拡張の payload `version` とは別物）

**本書の例はすべて `player-ban` 拡張（9 章）を題材にしている。** 仕様そのものは拡張に依存しない。`type` と payload のフィールド、および「実効化」が具体的に何を指すかは、各拡張がそれぞれの仕様で定める。

---

## 1. 用語

| 語                   | 意味                                                                                |
| -------------------- | ----------------------------------------------------------------------------------- |
| コンテンツ           | Akashic Engine で動くゲーム本体。拡張ライブラリをバンドルして配布される             |
| 拡張ライブラリ       | コンテンツにバンドルされる npm パッケージ。`g.game.external` を呼び、通知を受け取る |
| 実行基盤             | 下記 1.1 の定義による                                                               |
| プラグイン           | 実行基盤に登録され `g.game.external` にオブジェクトを生やす側の実装                 |
| active インスタンス  | Event を subscribe し Tick を publish する**役割**                                  |
| passive インスタンス | Tick を subscribe し Event を publish する**役割**                                  |

active / passive は**役割**であって、インスタンスの種別ではない。1 つのインスタンスが両方を兼ねてもよい。

### 1.1 実行基盤

次の 2 つを満たす実行環境を**実行基盤**と呼ぶ。

1. `@akashic/akashic-engine` 準拠のコンテンツを実行する 1 つ以上のインスタンスに対し、`@akashic/playlog` 準拠の Tick / Event を `@akashic/amflow` のプロトコルに沿って中継する
2. ゲーム開始時に、Event を subscribe しているインスタンスへ、送信者を `":akashic"` とする `{ "type": "start" }` の MessageEvent を送る

2 の `parameters.mode` が `"multi"` であるものを**マルチモード実行基盤**と呼ぶ。

### 1.2 この仕様が対象とする範囲

**本仕様はマルチモード実行基盤を対象とする。**

複数のプレイヤーが同じ場を共有することが前提の仕組みなので、単独プレイを前提とするモード（ランキングモードなど）は対象外とする。

## 2. 二方向とその非対称性

| 向き                  | 経路                                              | 性質                                                       |
| --------------------- | ------------------------------------------------- | ---------------------------------------------------------- |
| コンテンツ → 実行基盤 | `g.game.external.<key>` の関数呼び出しと callback | **ローカル**。呼んだインスタンスにしか結果が返らない       |
| 実行基盤 → コンテンツ | playlog への MessageEvent 注入                    | **決定的**。全インスタンスが同一 tick で同一内容を受け取る |

この非対称性が仕様の中心にある。

**ゲーム状態を変えてよいのは後者を受け取ったときだけ。** 前者の callback で状態を書き換えるとインスタンス間で state がずれる。callback はローカル UI（トーストを消す、ボタンを戻す）にだけ使う。

## 3. 名前空間

### 3.1 予約 playerId

通知イベントの発行者として、次の固定文字列を使う。

```
:multi-indiegame
```

- **実装者が誰であってもこの文字列のまま注入する。** XML の名前空間 URI と同じ扱いで、実行基盤の運営者が自分の org 名やサービス名に置き換えてはならない。置き換えると、その基盤の上でコンテンツが通知を受け取れなくなる
- playerId の 1 文字目に `:` が付くことはないので、予約語として使える
- `:akashic` は**実行基盤**が起動時の start 通知に使う予約 playerId（1.1）なので使わない
- **拡張ごとではなく名前空間として 1 つ。** 拡張の識別は `type` で行う。`:player-ban` のような機能名にはしない

### 3.2 type

payload の `type` は **npm パッケージ名をそのまま使う**。npm 名が一意なので、拡張が増えても衝突せず、採番表も要らない。

コンテンツ自身が `g.game.raiseEvent()` で送るメッセージとも、予約 playerId によって区別される。

## 4. 通知イベント（wire format）

playlog の MessageEvent として注入する。共通なのは `type` と `version` の 2 つで、それ以外のフィールドは `type` ごとに定義される。

例（`player-ban` の場合）:

```jsonc
[
  32, // playlog.EventCode.Message
  0, // eventFlags
  ":multi-indiegame", // 予約 playerId
  {
    "type": "@multi-indiegame/akashic-player-ban",
    "version": 1,
    // 以下は type ごとに定義されるフィールド
    "action": "banned",
    "playerId": "<対象の playerId>",
  },
]
```

- `type` と `version` は全ての payload に必須
- **時刻・乱数など非決定な値を載せてはならない。** 全インスタンスが同一内容を受け取ることが前提
- payload は JSON として直列化可能でなければならない

### 4.1 playerId の id 空間

payload がプレイヤーを指すときは、**実行基盤がそのコンテンツへ申告している playerId** を送る。コンテンツが `ev.player.id` や `g.game.selfId` で観測している値のことで、実行基盤の内部識別子（アカウント id、セッション id、cookie の値など）を送ってはならない。

コンテンツは自分が観測した id としか照合できないので、これが唯一マッチする値になる。

内部識別子をそのまま in-game playerId として配っている実行基盤では両者は一致するが、**一致するのは結果であって前提ではない**。たとえばゲストの内部 id を秘匿するために非可逆な派生値を in-game playerId として配る実行基盤では、通知にもその派生値を載せる。

生の内部 id か派生値かは問わない。判断基準は「**コンテンツが観測している id と同じか**」だけ。

## 5. version

`version` は **`type` ごとに独立**。ある拡張が v2 になっても、他の拡張は v1 のままでよい。拡張をまたいだ通し番号にはしない。

### 5.1 受信側の規約

受信側（拡張ライブラリ・実行基盤の双方）は、**自分が知る version と完全一致する payload だけを採用し、それ以外は黙って無視する**。エラーにしてはならない。

例（`player-ban` の場合）:

```ts
const TYPE = "@multi-indiegame/akashic-player-ban";
const KNOWN_VERSION = 1;

g.game.onMessage.add((ev) => {
  if (ev.player?.id !== RESERVED_PLAYER_ID) return; // 偽装除け
  const d = ev.data;
  if (d?.type !== TYPE) return; // 他拡張宛
  if (d.version !== KNOWN_VERSION) return; // 知らない版は黙って無視
  onPlayerBanned.fire({ playerId: d.playerId });
});
```

`>=` や `<=` ではなく**完全一致**であることが重要。理由は 5.4。

### 5.2 上げなくてよい変更 — フィールドの追加

例（`player-ban` の場合）:

```jsonc
{
  "type": "...player-ban",
  "version": 1,
  "action": "banned",
  "playerId": "u_123",
  "origin": "VIA_BLOCK",
} // origin が増えた
```

- 古いライブラリ: `origin` を読まない。従来どおり正しく動く
- 新しいライブラリ: `d.origin ?? "MANUAL"` と**必ず optional として読む**。古い実行基盤は送ってこない

判断基準は「**そのフィールドを無視した古い実装の挙動が、依然として正しいか**」。正しいなら据え置く。

### 5.3 上げるべき変更 — 既存フィールドの意味の変化

例（`player-ban` の場合）:

```jsonc
{
  "type": "...player-ban",
  "version": 2,
  "action": "banned",
  "playerId": "h_9f3a...",
} // フィールド名も型も同じ。値の意味だけが変わった
```

例: `playerId` を、コンテンツが観測している playerId から、部屋ごとにスコープした別のハッシュ id へ変える。4.1 の id 空間そのものが変わっている。

これを v1 のまま流すと、古いライブラリは知らない id を渡されて**誰にもマッチせず、その通知が静かに効かなくなる**。例外もログも出ない。**名前も型も変わっていないのに壊れる**、これが version を上げる典型。

逆に、実行基盤が in-game playerId の作り方を変え、通知の `playerId` もそれに追随しただけなら version は上げない。コンテンツから見れば `ev.player.id` と一致したままで、4.1 を満たし続けているため。**版を分けるのは id の作り方ではなく、コンテンツが観測している id と食い違うかどうか。**

フィールドの削除、optional から必須への変更も同じ扱い。

### 5.4 移行期間 — 並行送信

実行基盤は、同じ 1 件の確定について両方の version を流してよい。

例（`player-ban` の場合）:

```ts
sendEvent(
  buildNotificationEvent(TYPE, 1, {
    action: "banned",
    playerId: observedPlayerId,
  }),
);
sendEvent(
  buildNotificationEvent(TYPE, 2, { action: "banned", playerId: scopedId }),
);
```

- v1 だけ知るライブラリ → v1 を採用、v2 は未知なので無視
- v2 を知るライブラリ → v2 を採用、v1 は「未知」ではないが `KNOWN_VERSION` と不一致なので無視

受信側を `d.version <= KNOWN_VERSION` と書くと、新しいライブラリが v1 と v2 の**両方を採用して同じ確定を 2 回処理する**。ターン制ゲームならターンが二重に進む、という壊れ方をする。5.1 が完全一致である理由はこれ。

### 5.5 `type` にメジャー番号を入れない

`@multi-indiegame/akashic-player-ban-v2` のように、type にメジャー番号を足したものは作らない。type が増えると実行基盤側の分岐も増える。版は payload の中だけで表す。

## 6. 実行基盤が満たすべき契約

拡張を受け入れる実行基盤は、次を満たさなければならない。

### 6.A 通知の注入

確定した状態変化について、**Event を subscribe しているインスタンス（1 つ以上）へ、`@akashic/amflow` のインタフェースを通じて MessageEvent を届けること。**

全インスタンスへの伝播はここから自動的に従うので、別途の義務としては課さない。active インスタンスが受け取った Event は Tick に載り、passive インスタンスへ配られる。

**コンテンツが `g.game.raiseEvent()` で送ったイベントを傍受する方式では満たさない。** 実行基盤自身が、通常の経路とは別に注入すること。

> **なぜ傍受ではいけないか。** 傍受した時点でそのイベントは既に playlog に載っており、全インスタンスが処理し終えている。**要求を承認してよいか検証する余地が残らない。** 検証を挟むには、要求を通常の経路の外で受け取り、判断したうえで、結果を実行基盤の側から注入する必要がある。

payload の組み立ては、各拡張パッケージが提供する `buildNotificationEvent()` 系を使うこと。payload がプレイヤーを指すときは 4.1 の id 空間に従う。

### 6.B 状態変化の実効化

**通知を出したなら、その通知どおりの状態を実際に作ること。** コンテンツの中の演出だけに留めてはならない。

**どこまでやれば実効化と言えるかは、拡張ごとにその拡張の仕様が定める。** 実行基盤はそれを満たすこと。

> 例: `player-ban` では、対象がゲームの進行から外れるだけでなく、**そのセッションを閲覧もできない状態にすること**を求めている。

> **要求を拒否するのは自由。** 権限が無い、上限に達した、確認 UI で拒否された——いずれも実行基盤の判断でよい。この契約が縛るのは**通知を出したあと**であって、要求を必ず承認せよという意味ではない。
>
> **なぜ実効化まで求めるか。** これはゲームクリエイター向けの拡張である。ある基盤では実際に効くのに、別の基盤ではコンテンツの中だけの見せかけに終わる、という差があると、クリエイターは何を前提に作ればよいか分からなくなる。そのゲームで遊ぶプレイヤーからの苦情がクリエイターへ向かうことも起こりうる。**拡張が約束する効果をクリエイターに約束できるようにするため、実装者に負担を求める。**

### 6.C コンテンツ外で確定した変化の通知（条件付き）

管理画面やチャット UI など、**コンテンツの外で同種の状態変化を起こせる経路を持つなら、そこで確定した変化も 6.A と同じ経路で通知すること。**

そうした経路を持たない実行基盤には課さない。

> コンテンツは自分が要求していない変化も受け取ることになる。これが届かないと、実行基盤の側では状態が変わったのにコンテンツは何も知らないまま、という食い違いが残る。
>
> 例: `player-ban` で実行基盤のチャット UI から追放したのに通知が届かないと、実行基盤は対象を締め出しているのにコンテンツはターン順に残したまま、毎ターン待ち続けることになる。

### 6.D 権限は実行基盤が決める

**誰がその操作を発行してよいかは、実行基盤の決めごと**であって仕様には含めない。部屋主だけに許す基盤も、モデレーター権限を配る基盤も、全員に許す基盤もありうる。

したがって、

- **拡張ライブラリは権限を判定しない。** 呼ばれたらそのまま要求を投げ、可否は実行基盤の応答で知る
- **結果コードに基盤固有の役割名を持ち込まない。** 権限が無くて断られたことは `Unauthorized` 相当のひとつの理由で表す。`NotGameMaster` のような役割名を足すと、その役割を持たない実行基盤では意味を成さなくなる

判定を**どこで**行うかは 8 章（サーバー側に置くこと。クライアント側の判定は通信を減らすための最適化であって防御ではない）。ここで言っているのは、判定の**基準を誰が決めるか**の話。

ライブラリが権限を判定しようとすると、実行基盤ごとに違う役割をライブラリが知る必要が出る。たとえば「部屋主かどうか」を JoinEvent から割り出す実装にすると、コンテンツ側のハンドラ登録との順序で取れたり取れなかったりして、判定が不安定になる。

## 7. 拡張ライブラリが満たすべき契約

1. **feature detect して no-op で degrade すること。** `g.game.external.<key>` が無い環境（Akashic の headless runner や、この拡張に対応していない実行基盤。素の `akashic serve` もこれにあたる）で例外を出してはならない
2. **通知の採用前に発行者を検証すること。** `ev.player.id === ":multi-indiegame"` を確認してから payload を読む
3. **未知の `type` を黙って無視すること。** 複数の拡張を積んだコンテンツでは、各ライブラリが同じ `g.Scene#onMessage` に登録されるので、自分宛でない通知が必ず流れてくる
4. **通知の重複に耐えること。** 同じ確定が再送されうる。状態が実際に動いたときだけゲーム状態を変えること
5. **スナップショットの責務をコンテンツに明示すること。** 通知から積み上げた内部状態はイベント再生では復元されるが、スナップショット起動では欠落する
6. **受信を自前で担保すること。** MessageEvent は `g.Scene#onMessage` にしか届かず、Scene はコンテンツが自由に積み替える。Scene の遷移に追従して登録し直し、**コンテンツに「Scene を変えるな」という制約を課さないこと**
7. **受信し損ねても、次の呼び出しに影響しない作りにすること。** 内部状態が壊れて以降の要求が通らなくなる、という壊れ方をしてはならない

## 8. セキュリティ上の位置づけ

**プラグイン境界はセキュリティ境界ではない。**

コンテンツが実行基盤と同一オリジンで動く構成では、拡張ライブラリを経由せずに実行基盤の API を直接叩ける。したがって「ライブラリ経由でしか操作できない」という前提の設計は成り立たない。

権限判定・スコープ決定・上限・レート制限は、**すべて実行基盤のサーバー側**に置くこと。拡張ライブラリとプラグインは、呼び出しと通知を書きやすくするための道具であって、防御機構ではない。

## 9. 拡張レジストリ

| type                                  | version | リポジトリ                                                                  | external キー |
| ------------------------------------- | ------- | --------------------------------------------------------------------------- | ------------- |
| `@multi-indiegame/akashic-player-ban` | 1       | [akashic-player-ban](https://github.com/multi-indiegame/akashic-player-ban) | `playerBan`   |

**各拡張が自分の仕様で定めるもの**は、リポジトリ側にある。

- payload のフィールド（`type` / `version` 以外）
- `g.game.external.<key>` に生やす関数
- **6.B の「実効化」が具体的に何を指すか**

本書が定めるのは、拡張をまたいで共通する封筒・名前空間・版の扱い・実行基盤とライブラリの契約まで。

## 10. この仕様の変更

- 本文書の版（冒頭）は、契約そのものが変わったときだけ上げる
- 拡張の追加は 9 章に行を足すだけで、本文書の版は上げない
- 各拡張の payload `version` は、その拡張のリポジトリが管理する
